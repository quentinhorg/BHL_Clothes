
Lien du drive
https://docs.google.com/document/d/1UmmZH-7tIvOsV58ONVik2No-2Wc_DakHGyOzls1chsU/edit 



-----------
- améliorer la méthode ajout panier dans la BDD pour update la qte si deja une qte
- corriger bug qte panier en connecté
- corriger erreur aux mdp



-------

DELIMITER | 
    CREATE FUNCTION verifPaiementPanierActif(idClient int)
        RETURNS varchar(80)
        BEGIN
            DECLARE verifSolde int; DECLARE msg int ;

            SET verifSolde = ( REQ ) ;
            
            IF verifDisponibilite= 1 THEN 
                SET verif=  1;
            ELSE 
                SET verif=  0
            END IF;
        RETURN msg;
    END | 


    SELECT calcCmdTTC(c.num) FROM commande c WHERE c.idEtat = 1 AND c.idClient = 1;

------